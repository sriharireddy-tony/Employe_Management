import { NumsOnlyDirective } from './nums-only.directive';

describe('NumsOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NumsOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
